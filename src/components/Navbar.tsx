import React from "react";

// TODO: create navbar with logo in center and mod page links on the right?
const Navbar: React.FC = () => {

    return (
        <nav>
            navbar
        </nav>
    );
}

export default Navbar;
